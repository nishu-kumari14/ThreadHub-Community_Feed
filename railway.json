{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "cd backend && python manage.py migrate && gunicorn community_feed.wsgi --bind 0.0.0.0:$PORT",
    "numReplicas": 1
  }
}
